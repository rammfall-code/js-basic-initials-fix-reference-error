const login = prompt('Enter login');
const password = prompt('Enter password');

if (login === 'admin') {
  if (passwrd === 'qwerty1234') {
    alrt('Hello, admin');
  } else {
    alert('Incorrect info');
  }
} else if (logn === 'Guest') {
  if (password === 'guest666') {
    alert('Hello, guest');
  } else {
    alrt('Incorrect info');
  }
} else {
  alert('Go away');
}
